{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Cursor SoT for hooks is .cursor/hooks.json. This file must NOT list Claude-only dispatchers (claude-hook-dispatch / rule-router) — harness_audit fails closed on missing paths. Keep empty hooks so accidental Claude-shaped copies do not resurrect dead refs."
}
