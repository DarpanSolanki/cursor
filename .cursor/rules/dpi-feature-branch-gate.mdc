---
description: "DPI/DPIC/delayed payment interest — verify feature/delayed_payment_interest checkout on accounting-v2 + initial-setup before grep, KG, or RCA"
alwaysApply: true
---

# DPI feature branch gate (mandatory before code analysis)

**When the task mentions:** DPI, DPIC, delayed payment interest, dpiAccrual*, dpiBilling, DPI go-live, loan restructuring + DPI.

**Before grep, read processors, or trust `kg flow`/`cases`:**

1. Read `cursor-bundle/memory/reference_dpi_feature_branch.md`
2. Verify checkout (run in each repo):
   - `novopay-platform-accounting-v2` → `feature/delayed_payment_interest`
   - `novopay-platform-initial-setup` → `feature/delayed_payment_interest`
   - `novopay-platform-webapp` → `feature/delayed_payment_interest` (if UI)
3. `bash scripts/bin/kg-switch.sh` — KG must watermark accounting on feature branch, not `mfi_integration_*`
4. Optional: `bash scripts/bin/ensure-dpi-branches.sh` — prints PASS/FAIL

**Never** conclude "field/API missing" from `mfi_integration_*` checkout or `git show` alone while disk is on integration branch.

**Mixed workspace** (webapp on feature, accounting on integration) → wrong analysis. Fix branches first.
